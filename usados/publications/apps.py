"""Publications app."""

# Django
from django.apps import AppConfig


class PublicationsAppConfig(AppConfig):
    """Publications app config."""

    name = 'usados.publications'
    verbose_name = 'Publications'
